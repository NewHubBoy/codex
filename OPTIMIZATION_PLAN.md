# Web 前端性能优化计划

> 目标：降低首屏 JS 体积与关键渲染阻塞，改善路由切换响应速度。
> 日期：2026-02-05

## 进度概览

| # | 任务 | 状态 | 预期收益 |
|---|------|------|----------|
| 1 | 构建与包体优化（按需导入/包优化） | ✅ 已完成 | 减少初始包体 |
| 2 | antd 语言包按需加载 | ✅ 已完成 | 减少首屏 JS |
| 3 | 页面与组件拆分（动态导入） | 🚧 进行中 | 降低路由包体 |
| 4 | 服务器组件改造（减少 client JS） | ⏳ 待开始 | 降低首屏 JS |
| 5 | 资源与网络（图片/缓存/压缩） | ⏳ 待开始 | 缩短加载耗时 |
| 6 | 构建分析与基线追踪 | ⏳ 待开始 | 明确优化收益 |

## 已执行优化

### 1) 构建与包体优化
- 启用 `modularizeImports` 与 `optimizePackageImports`
- 防止全量引入 `antd` 与 `@ant-design/icons`
- 生产环境移除 `console`（保留 error）
- 关闭生产浏览器源码地图

### 2) antd 语言包按需加载
- 默认只打包 `zh-CN`
- 其它语言通过动态 import 触发加载

### 3) 页面与组件拆分（进行中）
- 详情页中的重型表格组件改为动态导入
  - `ActivityTable`
  - `AttachmentTable`

## 下一步执行清单

1. 运行构建分析，记录首屏 JS 与路由包体
2. 报表/大表格页继续拆分（避免首屏加载全部表格逻辑）
3. 将不需要交互的布局组件迁移为服务端组件
4. 检查静态资源与图片策略（优先 `next/image`）

## 验证方式

```bash
cd apps/web
pnpm build
# 可选：加入 bundle 分析
# pnpm build -- --analyze
```
